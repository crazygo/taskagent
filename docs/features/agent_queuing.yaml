feature: "Agent Concurrency and Queuing"
description: "Describes how the system handles concurrent agent execution across tabs and queuing within a single tab."

scenarios:
  - scenario: "Agents on different tabs execute in parallel"
    given:
      - "The user is on the 'Blueprint' tab and has submitted a long-running prompt."
      - "The 'Blueprint' agent is currently processing."
    when:
      - "The user switches to the 'Glossary' tab and submits a new prompt."
    then:
      - "The 'Glossary' agent starts processing the new prompt immediately."
      - "The 'Blueprint' agent and 'Glossary' agent run concurrently."

  - scenario: "Prompts for the same tab are queued"
    given:
      - "The 'Blueprint' agent is currently processing a prompt on the 'Blueprint' tab."
    when:
      - "The user submits another prompt to the 'Blueprint' tab."
    then:
      - "The new prompt is added to the 'Blueprint' tab's independent queue."
      - "The new prompt starts only after the first 'Blueprint' prompt is complete."
