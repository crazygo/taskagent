# Copy this file to `.env.local`, fill in the required values, and place it either in the repository root
# or under your workspace path (e.g. {workspace}/.askman/.env.local) before running TaskAgent.

# Enable streamed reasoning tokens (applies to OpenRouter + compatible OpenAI endpoints)
AI_REASONING_ENABLED=true

# --- Option A: OpenRouter credentials ---
# OPENROUTER_API_KEY='sk-or-**'
# OPENROUTER_MODEL_NAME='z-ai/glm-4.6'
# OPENROUTER_BASE_URL='https://openrouter.ai/api/v1'

# --- Option B: OpenAI-compatible endpoint (e.g. BigModel/GLM) ---
OPENAI_API_KEY='**'
OPENAI_MODEL_NAME='glm-4.6'
OPENAI_API_BASE_URL='https://open.bigmodel.cn/api/coding/paas/v4'

# Coder Agent (Used by Claude Agent SDK)
ANTHROPIC_API_KEY='**'
ANTHROPIC_BASE_URL='https://open.bigmodel.cn/api/anthropic'
ANTHROPIC_MODEL='glm-4.6'
